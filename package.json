{
  "name": "press-ticket",
  "version": "1.0.0",
  "private": true,
  "description": "Press-Ticket monorepo (backend + frontend) deployed via Railpack",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "echo \"root build is handled by railpack steps\""
  }
}
